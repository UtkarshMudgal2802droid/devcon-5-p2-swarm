import { EthAddress, Identifier } from "@ethersphere/bee-js";
import type { Bee, BeeRequestOptions, DownloadOptions } from "@ethersphere/bee-js";
import type { UploadProgress } from "./types";
import type { SingleOwnerChunk } from "../types";
/**
 * The slice of a Bee client that reading chunks needs.
 *
 * Everything that only downloads — the feed finders and the chunk readers
 * here — takes this rather than a whole `Bee`, so a test can hand in a
 * store-backed mock without casting, and the signature says which of the
 * client's surface the caller actually depends on. `url` is there for the
 * log lines only.
 */
export type ChunkDownloader = Pick<Bee, "downloadChunk" | "url">;
/**
 * Download data using only the chunk API
 * This ensures encrypted data remains encrypted during transmission and avoids metadata leakage
 *
 * Supports both:
 * - Regular references (64 hex chars = 32 bytes)
 * - Encrypted references (128 hex chars = 64 bytes: 32-byte address + 32-byte encryption key)
 */
export declare function downloadDataWithChunkAPI(bee: ChunkDownloader, reference: string, _options?: DownloadOptions, onProgress?: (progress: UploadProgress) => void, requestOptions?: BeeRequestOptions): Promise<Uint8Array>;
export declare function downloadSOC(bee: ChunkDownloader, owner: string | Uint8Array | EthAddress, identifier: string | Uint8Array | Identifier, requestOptions?: BeeRequestOptions): Promise<SingleOwnerChunk>;
export declare function downloadEncryptedSOC(bee: ChunkDownloader, owner: string | Uint8Array | EthAddress, identifier: string | Uint8Array | Identifier, encryptionKey: string | Uint8Array, requestOptions?: BeeRequestOptions): Promise<SingleOwnerChunk>;
//# sourceMappingURL=download-data.d.ts.map